import { Prose } from '@/components/os/Prose'

export function TrashApp() {
    return (
        <Prose>
            <h1>Trash</h1>
            <p>Empty. Nothing has been deleted.</p>
            <p>
                This exists because the metaphor needs somewhere for things to go. An OS
                desktop with no trash is a set of links wearing icons: the affordances have
                to be complete or the illusion reads as decoration.
            </p>
        </Prose>
    )
}
